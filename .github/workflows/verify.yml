# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: Verify

on:
  push:
    branches:
      - main
      - "pull-request/[0-9]+"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  verify:
    name: Verify generated files, go.mod, and license headers
    runs-on: linux-amd64-cpu16
    if: github.repository == 'NVIDIA/cluster-readiness-engine'
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Run verification checks
        run: make verify

      # Fails the build when any Go dependency carries a forbidden or
      # restricted license, so an incompatible module cannot land silently.
      # GOROOT is set explicitly: `go run pkg@version` builds with -trimpath,
      # which empties the binary's baked-in GOROOT, and go-licenses v1.6.0
      # then misclassifies every stdlib package and exits non-zero.
      - name: Dependency license scan
        run: GOROOT="$(go env GOROOT)" go run github.com/google/go-licenses@v1.6.0 check ./... --disallowed_types=forbidden,restricted
